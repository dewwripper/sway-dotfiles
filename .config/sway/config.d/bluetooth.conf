# Float blueman-manager and set a comfortable popup size
for_window [app_id="blueman-manager"] {
    floating enable
    resize set width 700px height 500px
    move position center
}

# Keyboard shortcut to toggle/open blueman-manager
bindsym $mod+Shift+b exec blueman-manager

# Status Bar (Quickshell)
# Quickshell manages its own QtQuick/QML layer-shell surface.
# Using -n (--no-duplicate) ensures only a single instance runs and restarts if needed on reload.
exec_always quickshell -n

